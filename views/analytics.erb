<h3>You just generate the url: <a href="<%= @shorten_url %>"><%= @shorten_url %></a></h3>
<div>
  <h4>Some analytics about your shortened url:</h4>
  <div class="analytics-data">
    <div class="number-of-access">
      <strong><%= @shortened_url.access_to_url.size %></strong>
      <sub>accesses until now</sub>
    </div>
    <div class="browser-counter"></div>
  </div>
</div>
